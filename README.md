# auth-log-analyzer
Work in progress.

Parses Linux auth.log to report SSH login attempts, failures and top source IPs

## Requirements

Python 3.14.4

No external dependencies

## Usage

python3 analyzer.py

## What it does not do yet

currently it only extracts the IP address in the logs